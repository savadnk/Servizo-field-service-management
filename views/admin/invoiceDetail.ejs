<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Invoice <%= invoice._id %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet" />
  <style>
    :root {
      --bg: #000000; 
      --panel: #151821; 
      --row: #1a1d24; 
      --muted: #9aa3b2; 
      --line: #232735;
      --chip: #222733; 
      --chipText: #d8deea; 
      --green: #22c55e; 
      --blue: #3b82f6; 
      --card: #151821;
      --amber: #f59e0b; 
      --cyan: #22d3ee; 
      --danger: #ef4444; 
      --txt: #e8ecf4; 
      --grey: #4b5563;
      --text-100: #e8f1ee; 
      --text-300: #a7b0ad; 
      --muted-600: #4d4f55; 
      --muted-500: #5a5c62; 
      --brand-500: #16da64;
      --brand-600: #12b754; 
      --focus: #86f3b1; 
      --chip-border: #284430; 
      --green-dim: #16a34a; 
      --surface: #111418;
    }
    
    a {
      text-decoration: none;
    }
    body {
      background-color: var(--bg);
      font-family: "Poppins", sans-serif;
      color: var(--txt);
    }
    .invoice-box {
      max-width: 800px;
      margin: 40px auto;
      background: var(--panel);
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
      padding: 40px;
      position: relative;
      overflow: hidden;
      border: 1px solid var(--line);
    }
    .invoice-box::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 4px;
      background: linear-gradient(90deg, var(--blue), var(--cyan));
    }
    .invoice-header {
      border-bottom: 2px solid var(--blue);
      padding-bottom: 10px;
      margin-bottom: 25px;
    }
    .company-name {
      font-size: 1.4rem;
      font-weight: 600;
      color: var(--blue);
    }
    .invoice-title {
      text-align: right;
      font-weight: 700;
      font-size: 1.6rem;
      color: var(--txt);
    }
    .details-table {
      color: var(--txt);
      border-color: var(--line) !important;
    }
    .details-table td {
      padding: 8px 4px;
      border-color: var(--line) !important;
      background-color: var(--row) !important;
      color: var(--txt);
      
    }
    .details-table th {
      background-color: var(--row) !important;
      color: var(--txt);
      border-color: var(--line) !important;
    }
    .summary {
      border-top: 2px solid var(--line);
      padding-top: 10px;
    }
    .status {
      font-weight: 600;
      color: <%= invoice.status === "Paid" ? "var(--green)" : "var(--danger)" %>;
    }
    .btn-print {
      margin-top: 25px;
    }
    .invoice-number {
      background: var(--chip);
      padding: 5px 10px;
      border-radius: 4px;
      font-weight: 600;
      color: var(--chipText);
    }
    .payment-status {
      display: inline-block;
      padding: 5px 15px;
      border-radius: 20px;
      font-weight: 600;
      font-size: 0.9rem;
    }
    .paid-status {
      background-color: rgba(34, 197, 94, 0.1);
      color: var(--green);
      border: 1px solid var(--chip-border);
    }
    .unpaid-status {
      background-color: rgba(239, 68, 68, 0.1);
      color: var(--danger);
      border: 1px solid rgba(239, 68, 68, 0.3);
    }
    .total-amount {
      font-size: 1.2rem;
      color: var(--brand-500);
    }
    .bg-light {
      background-color: var(--row) !important;
      border-color: var(--line) !important;
      color: var(--txt);
    }
    .text-primary {
      color: var(--blue) !important;
    }
    .text-muted {
      color: var(--muted) !important;
    }
    .btn-outline-primary {
      color: var(--blue);
      border-color: var(--blue);
    }
    .btn-outline-primary:hover {
      background-color: var(--blue);
      color: var(--txt);
    }
    .btn-danger {
      background-color: var(--danger);
      border-color: var(--danger);
    }
    .btn-danger:hover {
      background-color: rgba(239, 68, 68, 0.8);
      border-color: rgba(239, 68, 68, 0.8);
    }
    
    /* Print styles */
    @media print {
      :root {
        --bg: white; 
        --panel: white; 
        --txt: #000000; 
        --line: #cccccc;
      }
      
      body {
        background-color: white;
        font-size: 12pt;
        color: black;
      }
      .invoice-box {
        box-shadow: none;
        margin: 0;
        padding: 20px;
        max-width: 100%;
        border: 1px solid #ccc;
      }
      .no-print {
        display: none !important;
      }
      .btn-print, .btn-success, .btn-outline-secondary, .btn-danger {
        display: none !important;
      }
      .invoice-box::before {
        height: 3px;
      }
      .bg-light {
        background-color: #f8f9fa !important;
      }
      .details-table {
        color: black;
      }
      .details-table th {
        background-color: #f8f9fa !important;
        color: black;
      }
    }
  </style>
</head>

<body>
  <div class="invoice-box">
    <div class="d-flex justify-content-between invoice-header">
      <div>
        <div class="company-name"><%= invoice.admin.companyName || "Agency Name" %></div>
        <small class="text-muted"><%= invoice.admin.email %></small><br />
        <small class="text-muted">Date Issued: <%= invoice.issuedDate.toDateString() %></small>
      </div>
      <div class="invoice-title">
        INVOICE
        <div class="invoice-number mt-1">#<%= invoice._id.toString().slice(-6).toUpperCase() %></div>
      </div>
    </div>

    <div class="row mb-4">
      <div class="col-md-6">
        <h6 class="text-primary">Bill To:</h6>
        <div class="border rounded p-3 bg-light">
          <p class="mb-1"><b><%= invoice.customer.name || "Customer Name" %></b></p>
          <p class="mb-0 text-muted"><%= invoice.customer.email %></p>
        </div>
      </div>
      <div class="col-md-6 text-end">
        <h6 class="text-primary">Service Info:</h6>
        <div class="border rounded p-3 bg-light">
          <p class="mb-1">Job: <b><%= invoice.job.serviceType || "Service" %></b></p>
          <p class="mb-0">
            Status: 
            <span class="payment-status <%= invoice.status === "Paid" ? "paid-status" : "unpaid-status" %>">
              <%= invoice.status %>
            </span>
          </p>
        </div>
      </div>
    </div>

    <table class="table table-bordered details-table text-light">
      <thead class="">
        <tr>
          <th>Description</th>
          <th class="text-end">Amount (₹)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td><%= invoice.description || "Service Charge" %></td>
          <td class="text-end">₹<%= invoice.serviceAmount.toFixed(2) %></td>
        </tr>
        <tr>
          <td>Platform Fee (10%)</td>
          <td class="text-end">₹<%= invoice.platformFee.toFixed(2) %></td>
        </tr>
      </tbody>
      <tfoot>
        <tr class="summary">
          <td><b>Total</b></td>
          <td class="text-end total-amount"><b>₹<%= invoice.totalAmount.toFixed(2) %></b></td>
        </tr>
      </tfoot>
    </table>

    

    <div class="text-center no-print">
      <% if (invoice.status === "Unpaid") { %>
        <div class="mt-4">
          <span class="payment-status unpaid-status p-2">
            <i class="bi bi-exclamation-circle"></i> Pending Payment
          </span>
        </div>
      <% } else { %>
        <div class="mt-4">
          <span class="payment-status paid-status p-2">
            <i class="bi bi-check-circle"></i> Payment Completed
          </span>
        </div>
      <% } %>
    </div>

    <div class="text-center no-print">
      <button class="btn btn-outline-primary btn-print" onclick="window.print()">
        <i class="bi bi-printer"></i> Print / Download PDF
      </button>
    </div>
    
    <div class="text-center mt-3 no-print">
      <button class="btn btn-danger mx-auto">
        <a href="/admin/invoices" class="text-white">
          <i class="bi bi-house"></i> Back to Invoices
        </a>
      </button>
    </div>
  </div>
</body>
</html>